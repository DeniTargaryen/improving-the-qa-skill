# План: после недели 1 (2026-08-10, Sonnet 5)

**Статус:** заменяет операционную часть `docs/superpowers/plans/2026-08-05-post-week0-aqa-plan.md` для недели 2+ (структура блоков 2.x/3.x/4.x из старого плана остаётся источником тем — этот файл фиксирует, что из неё уже открыто, что дальше и как это стыкуется с календарём).

**База:** неделя 1 (1.1–1.4) ✅ сдана 2026-08-10. Ревью — `docs/sonnet_reviews/2026-08-10-week1-and-1.4-review.md`.

---

## 1. Что открыто прямо сейчас

| Блок | Папка | Статус |
|------|-------|--------|
| **2.1 Фикстуры** | `02_pytest_craft/2.1_fixtures/` | ▶️ активный, следующий для ученика |
| 2.2 Моки | `02_pytest_craft/2.2_mocks/` | 🔒 каркас готов (TASK.md + Track A stub/тест), не открывать до сдачи 2.1 |
| 2.3 Сильные ассерты + parametrize | не создано | 🔒 создаётся отдельным пакетом, когда подойдёт очередь (после 2.2) |
| 2.4 Drill маркеры/конфиг | не создано | 🔒 то же |

Почему именно 2.1 первым: по плану `2026-08-05-post-week0-aqa-plan.md` это Track B и логически идёт первым в неделе 2 — закрывает разрыв «фикстура отдаёт строку» (0.3) и настоящий setup/teardown, который у ученика уже есть в мышечной памяти с работы, но не тренировался осознанно с нуля. Также не требует предварительного 2.2 (моки), в отличие от порядка наоборот.

## 2. Порядок блоков 2.x дальше (без изменений от старого плана по темам, меняется только то, что теперь это фиксирует Sonnet, а не коуч)

1. **2.1** Фикстуры (yield/teardown, scope=module, фабрика) — открыт.
2. **2.2** Моки (`unittest.mock`, переписать тест 0.4 на mock, опционально `requests-mock`) — каркас готов, откроется по гейту.
3. **2.3** Сильные ассерты (`in`+`isinstance` вместо `is not None`, `parametrize` с `ids=`, негативный `parametrize`) — TASK ещё не написан, будет отдельным пакетом Sonnet, когда 2.2 сдан (не откладывать надолго — это быстрый блок).
4. **2.4** Drill маркеры/конфиг pytest — устно + короткий код, TASK ещё не написан.

**Гейт недели 2 → неделя 3** (`03_api_framework`, CRUD в `BaseApi`, свои исключения в API, негатив+схема, unit-тесты слоёв на моках) — не открывать, пока 2.1–2.4 не сданы. Планирование недели 3 в деталях — отдельным пакетом Sonnet ближе к делу (не сейчас, чтобы не плодить TASK впрок, которые могут устареть по фидбеку с 2.x).

## 3. Как это стыкуется с `CALENDAR.md`

- Календарь (`CALENDAR.md`, корень) расписывает 2.1→2.4 по дням на 3 недели вперёд (10–30 августа) с учётом рабочей нагрузки после смены и обязательных выходных.
- Если по факту темп ученика отличается от календаря (быстрее/медленнее) — это нормально, календарь ориентир по нагрузке, не жёсткий дедлайн. Гейты (сдача блока = зелёные тесты + критерии TASK) важнее дат.
- Долги/повторы (декораторы-retry, `is`/`==`, `copy`) идут **параллельно** новым блокам 2.x, не вместо них — расписаны в `CALENDAR.md`, раздел «Долги».

## 4. Что отложить (осознанно, не забыть, но не сейчас)

- **TASK для 2.3 и 2.4** — писать отдельным пакетом, когда подойдёт очередь по гейту. Причина: если написать всё сразу на 3 недели вперёд, а фидбек по 2.1/2.2 покажет системную проблему в формате TASK — придётся переписывать уже готовые 2.3/2.4 задания, лишняя работа.
- **Неделя 3 (`03_api_framework`) в деталях** — тема зафиксирована в старом плане (`2026-08-05-post-week0-aqa-plan.md`), но конкретные TASK не пишутся, пока не закрыта неделя 2.
- **Правки TASK недели 1 (1.1–1.4) по фидбеку** — разрешены методистом (см. `HANDOFF.md`), но не сделаны в этом пакете сознательно: приоритет отдан новым 2.x блокам, как явно попросил ученик («главное — новые темы»). Если ученик явно попросит точечную правку старого TASK — это можно сделать отдельно, не блокируя движение вперёд.
- **Глубокое ревью 0.5 (interview voice, ANSWERS)** — по запросу ученика, не в этом пакете.
- **Резюме — доработка формулировок под конкретные вакансии** — `docs/resume/RESUME_NOTES_2026-08-10.md` даёт универсальные формулировки; адаптация под конкретную вакансию (если попросит) — отдельная итерация.

## 5. Гейт сдачи 2.1 (для коуча, чтобы не сочинять свой)

- `python -m pytest 02_pytest_craft/2.1_fixtures -v` — все тесты зелёные.
- Все три фикстуры (А, Б, В) находятся в `conftest.py`, не в файлах тестов.
- Упражнение Б реально доказано в тесте (через `id()` или счётчик `calls`), не просто написано `scope="module"` без проверки — это отдельно прописано в TASK.md, часть Б, «частая ошибка».
- Упражнение В — фикстура возвращает функцию (`Callable`), а не готовый список постов.

Когда всё это выполнено — сдача 2.1, коуч обновляет `PROGRESS.md`, открывает 2.2 (уже готов, лежит в репозитории, ждёт гейта).
