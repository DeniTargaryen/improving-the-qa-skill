# План: 15.08 → ~08.09.2026 (пролонгация плана 12.08, с рыночной правкой по мокам)

**Статус:** дополняет `docs/superpowers/plans/2026-08-12-asap-two-plus-weeks-plan.md` (тот план не отменяется — 2.1–3.2 по нему сданы досрочно). Этот план закрывает горизонт от текущей точки (15.08, 3.3 не начат) до начала сентября, с двумя новыми блоками кода и правкой статуса `unittest.mock` по факту рыночного ревью (`docs/sonnet_reviews/2026-08-15-market-vs-student-feedback.md`).

**Цель ученика без изменений:** работа AQA Python middle / Fullstack QA **как можно быстрее**. Всё ниже приоритизировано по этому критерию.

---

## 1. Текущий статус на 15.08.2026 (честно, без вранья задним числом)

| Блок | Статус |
|------|--------|
| 1.1–1.4, 2.1–2.4 | ✅ все сданы (см. `PROGRESS.md`) |
| 3.1 CRUD | ✅ сдано 13.08 |
| 3.2 исключения + негатив | ✅ сдано 13.08 |
| **3.3 юниты на моках** | ▶️ **активный, НЕ написан** (только заготовка, `test_unit_mocks.py` пустой) — статус вернулся на **обязательный, короткий** (правка вердикта дневного пакета 15.08, факты — `2026-08-15-market-vs-student-feedback.md`) |
| **3.4 Pydantic-модель + check_values** | 🔓 новый, TASK готов — `03_api_framework/3.4_pydantic_schedule_check/TASK.md` |
| **3.5 retry-хелпер через мок** | 🔓 новый, TASK готов — `03_api_framework/3.5_retry_helper_with_mock/TASK.md` |
| 4.1–4.3 (буфер) | 🔓 TASK готовы, опциональны — без изменений |

**Важное отличие от дневного пакета 15.08:** тот пакет ошибочно пометил в `CALENDAR.md` строку 15.08 как «✅ 3.3» — ученик на тот момент **решил** писать 3.3, но код не написал. Это исправлено везде ниже (`CALENDAR.md`, `PROGRESS.md`).

## 2. Порядок блоков — таблица недель

| Неделя | Даты | Блоки | Почему в этом порядке |
|--------|------|-------|--------------------------|
| 3 (продолжение) | 15–19 августа | 3.3 → 3.4 | 3.3 первым — уже начат по факту решения ученика, короткий (3–4 теста без сети). 3.4 сразу после — тот же принцип (Pydantic-модель без сети), но на его сильной теме (перенос реального рабочего паттерна) — закрепляет уверенность после 3.3 |
| 3 (хвост) | 20–23 августа | 3.5 | Отдельно от 3.3/3.4 — сложнее по механике (мок + патч конкретного пути + `side_effect` списком), нужен день без спешки; плюс закрывает долг декораторов/retry новым входом |
| 4 (опционально, буфер) | 24–30 августа | 4.1 Allure → 4.2 Timer → 4.3 Voice round 2 | Без изменений от плана 12.08 — только если есть запас времени |
| Резерв/дособес | 31 августа – 8 сентября | Повторы долгов (устный банк) + собесы | Трек по факту закрыт — время на отклики/собесы |

**Гейт недели 3 (обновлён, отменяет дневную правку 15.08):** 3.1 + 3.2 + **3.3** обязательны (рыночная правка вернула 3.3 в обязательный статус). 3.4/3.5 — тоже обязательны как часть этого пакета (они новые, не были в гейте 12.08, но заменяют собой «просто повторить теорию» дешёвым кодом, который уже написан за ученика в частях, требующих только тестов/моделей, не полного алгоритма).
**Гейт «трек закрыт, фокус на собесах»:** 3.1–3.5 сданы, неделя 4 — по факту доступного времени.

## 3. Явная привязка к рынку (что изменилось с 12.08)

Полный разбор — `docs/sonnet_reviews/2026-08-15-market-vs-student-feedback.md`. Короткая версия:

- Вакансия ИнфоТеКС (AQA Python, Уфа) и вакансия СберОС (AQA Python) в 2026 явно перечисляют «pytest (мок-объекты)»/«pytest (mock)» в списке требуемых навыков — рядом с декораторами и аннотациями типов (его же слабые темы). Это меняет статус `unittest.mock` с «опционально» обратно на «обязательно, но коротко».
- `interview-prep.coderboi.com` (обновление 2026-06-18) и `medhly.com` держат отдельную устойчивую категорию вопросов про `Mock`/`patch`/golden rule «patch где используется» — то есть это не разовая тема, а стабильная часть банков вопросов текущего года.
- Mountebank/service virtualization остаётся более редким и ценным навыком (не пересматривается) — но это не аргумент против `unittest.mock`, это два разных, оба нужных уровня ответа.

## 4. Новые блоки — что каждый даёт и почему не повтор старого

| Блок | Новый навык относительно 3.3 | Почему не «то же самое ещё раз» |
|------|-------------------------------|-----------------------------------|
| **3.4** | Pydantic-модель с собственным методом `check_values`, список вложенных моделей (`entries: list[Entry]`) | 3.3 работал с готовыми клиентом/функциями и `dict`-проверкой; 3.4 — построение своей модели данных, перенос его реального рабочего паттерна (Schedule/Entry), новый навык — Pydantic-моделирование, не мокирование |
| **3.5** | `Mock(side_effect=[...])` со списком (не одно `return_value`), `patch` на **другую** зависимость (`retry_helper.time.sleep`, не `requests.get`), тестирование retry-логики без реального ожидания | Новый вход для памяти на `patch` (другой путь патчинга, другой сценарий — не HTTP, а внутренний `time.sleep`) + закрывает долг декораторов/retry (1.3) без повторной травмы «пиши retry с нуля» — теперь ретрай дан готовым, тренируется тестирование, а не написание |

## 5. Что опционально / что отложено (без изменений от 12.08, если не сказано иначе)

- Неделя 4 целиком — буфер, не обязательна.
- 2.2-Б/В — остаются скипнутыми.
- Ручная JSON-схема с нуля — не возвращается отдельным блоком.
- `autospec`, `MagicMock`-специфика, `AsyncMock`, `mock_open` — только в устном банке вопросов, не отдельным блоком кода (обосновано в `2026-08-15-market-vs-student-feedback.md` §8).
- Второй проход "перепиши retry с нуля" (1.3) — сознательно НЕ возвращается; вместо этого — 3.5, тестирование готового retry, другой навык.

## 6. Как это стыкуется с `CALENDAR.md`

`CALENDAR.md` переписан от 15.08 с учётом: правки лжи про «3.3 сделано», добавления 3.4/3.5 в расписание, интеграции устных долговых слотов (JWT/типы/auth) без задваивания нагрузки, актуального гейта недели 3.

## 7. Правило на будущее (без изменений)

Отзыв ученика — сигнал, что нужно проверить, не приказ переписать план вслепую. В этот раз проверка фактами вернула часть решения назад (3.3 обязателен) — это нормальный результат методической проверки, не «ученик был неправ» и не «прошлый пакет был неправ» — оба правы частично, вердикт синтезирует оба взгляда с опорой на источники, не на интуицию.
