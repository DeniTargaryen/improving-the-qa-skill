# План ASAP: 12.08 → ~08.09.2026 (недели 2–4, полностью)

**Статус:** заменяет `docs/superpowers/plans/2026-08-10-post-week1-sonnet-plan.md` (та политика «TASK потом отдельным пакетом» отменена явным требованием ученика 12.08 — признана провалом операционки, разбор в `docs/sonnet_reviews/2026-08-12-package-failure-and-market-review.md`).

**Цель ученика:** получить работу AQA Python middle **как можно быстрее**. Всё ниже приоритизировано по этому критерию, не по учебной полноте — что рынок слабо подтвердил, вырезано или помечено опционально (полный разбор — `docs/sonnet_reviews/2026-08-12-task-self-review-vs-real-interviews.md`).

**Все блоки ниже уже физически лежат в репозитории с полными `TASK.md` и самодостаточной инфраструктурой** (не «каркас», не «потом») — коуч ведёт по ним без ожидания следующего пакета Sonnet минимум до конца недели 4/буфера.

---

## 1. Текущий статус на 12.08.2026

| Блок | Статус |
|------|--------|
| 2.1 Фикстуры | ✅ сдано 11.08 |
| 2.2 Моки | ✅ сдано 12.08 с оговоркой (А сделана, Б/В осознанно скипнуты — подтверждено рыночным ревью, см. `docs/sonnet_reviews/2026-08-12-package-failure-and-market-review.md`, раздел 3) |
| **2.3 Сильные ассерты + parametrize** | ▶️ **следующий активный блок** — `02_pytest_craft/2.3_strong_asserts/TASK.md` |
| 2.4 Маркеры/конфиг | 🔓 открыт, TASK готов — `02_pytest_craft/2.4_markers_and_config/TASK.md` |
| 3.1 CRUD в `BaseApi` | 🔓 открыт, TASK готов — `03_api_framework/3.1_crud/TASK.md` |
| 3.2 Свои исключения + негатив | 🔓 открыт, TASK готов — `03_api_framework/3.2_exceptions_and_negative/TASK.md` |
| 3.3 Unit-тесты на моках | 🔓 открыт, TASK готов — `03_api_framework/3.3_unit_tests_with_mocks/TASK.md` |
| 4.1–4.3 (буфер) | 🔓 открыты, TASK готовы, **опциональны** — `04_interview_theory/` |

**Важно про 🔓 vs ▶️:** все блоки **написаны и лежат в репо** прямо сейчас (это и было главным требованием пакета 12.08) — но по правилу гейта коуч всё равно ведёт **по одному активному блоку**, не разрешает прыгать вперёд без сдачи текущего. Разница с прошлым пакетом: раньше «закрыто до гейта» означало «TASK физически не существует», теперь — «TASK готов, просто не начат по порядку». Ученик может посмотреть на весь план вперёд (это и есть требование прозрачности), но решает блоки последовательно.

## 2. Порядок блоков — таблица недель

| Неделя | Даты | Блоки | Почему в этом порядке |
|--------|------|-------|--------------------------|
| 2 (хвост) | 12–17 августа | 2.3 → 2.4 | Оба короткие, закрывают неделю 2 быстро — 2.3 чуть тяжелее (parametrize+ассерты), 2.4 почти весь устный |
| 3 | 18–24 августа | 3.1 → 3.2 → 3.3 | CRUD первым (самое объёмное, живая логика клиента), исключения вторым (короче, использует тот же клиент по смыслу), моки третьим (закрепляет 2.2 на реальном слое — логично, что после того, как слой уже написан в 3.1/3.2 по духу, хотя технически 3.3 использует свою независимую копию) |
| 4 (опционально, буфер) | 25–31 августа | 4.1 Allure → 4.2 Timer → 4.3 Voice round 2 | Только если недели 2–3 закрыты с запасом. Если нет времени — пропустить целиком с устной оговоркой (см. TASK каждого блока), это НЕ блокирует поиск работы |
| Резерв/дособес | 1–8 сентября | Повторы долгов + собесы | К этому моменту трек закрыт по факту — время уходит на реальные отклики/собесы, не на новый учебный материал |

**Гейт недели 2 → 3:** 2.3 и 2.4 сданы (тесты зелёные для 2.3, устные ответы + правка `pytest.ini` для 2.4).
**Гейт недели 3 → 4/буфер:** 3.1–3.3 сданы (все три папки — тесты зелёные).
**Гейт «трек закрыт, можно фокусироваться только на собесах»:** недели 2–3 сданы, неделя 4 — по факту доступного времени, не обязательна.

## 3. Явная привязка к целевым компаниям (Ozon/Avito/VK/Альфа-Банк и подобные)

Короткая версия — полный разбор с источниками в `docs/sonnet_reviews/2026-08-12-task-self-review-vs-real-interviews.md`.

- **Avito 2026** (`enigmai.ru`): технический скрининг — простые задачи Python/SQL; deep dive — «автоматизация, API, инфраструктура, микросервисы»; секция автоматизации прямо просит «как мокать внешние зависимости (например, платёжный шлюз)» и «написать тест для эндпоинта... как мокать внешние зависимости» — это 2.2/3.3 буквально.
- **Ozon Tech (WMS, реальная вакансия AQA Python)**: «разрабатывать утилиты/инструменты и моки», gRPC/Kafka «будет плюсом» — подтверждает и 3.3 (моки как рабочий навык), и ценность резюме-правок Kafka/gRPC (`docs/resume/RESUME_NOTES_2026-08-10.md`).
- **Общий рынок 2025 (Хабр, рынок труда)**: живое кодирование — ООП, SQL, Docker/Linux, интеграция CI/CD; фреймворки — pytest конкретно называется как ожидаемый навык.
- **Банк реальных вопросов QA Automation Engineer (smartiqa.ru, 09.2025)**: буквально совпадает с 2.1 (scope фикстур, порядок вызова), 2.3 (`parametrize`, `pytest.raises`), 2.4 (несколько `conftest.py`, hooks) — это не абстрактная теория, это записанные вопросы с реального опыта соискателя.

## 4. Что опционально / что отложено

- **Неделя 4 целиком** — буфер. Открыта структурно (TASK готовы), но НЕ обязательна для «готовности к собесу» по мнению рыночного ревью — Allure/таймер/voice закрывают точечные риски, не core-навыки.
- **Ручная JSON-схема validate_post_schema как отдельный блок** — вырезана из плана (была в старом плане 05.08 как часть 3.2), причина — рынок не подтвердил как отдельно запрашиваемый навык, см. таблицу в `docs/sonnet_reviews/2026-08-12-task-self-review-vs-real-interviews.md`. Осталась только устная оговорка внутри TASK 3.2, не отдельный код.
- **2.2-Б/В (mock вместо week0-теста, requests-mock)** — остаются скипнутыми, не переоткрываются.
- **Правки TASK недели 1 (1.1–1.4)** — не в этом пакете; приоритет — контент 2.x–4.x, как явно попросил ученик.
- **Неделя 5+/что-то за пределами этого плана** — не планируется заранее; когда недели 2–4 реально сданы, будет отдельный пакет по факту (но не с политикой «TASK потом» — контент к моменту сдачи текущего горизонта должен уже быть готов заранее, минимум на 2 недели, это теперь стандарт, не разовое исключение).

## 5. Как это стыкуется с `CALENDAR.md`

`CALENDAR.md` (корень) переписан от 12.08 на **~4 недели вперёд** (до начала сентября) под блоки из этого плана, с реалистичной нагрузкой (20–40 минут будни, отдых по выходным) и с разделом «Долги» (повторы retry/is-==/copy), который идёт параллельно, не вместо нового материала.

## 6. Правило на будущее (чтобы 10.08 не повторилось)

Каждый следующий пакет Sonnet обязан:
1. Покрывать TASK минимум на **2 недели вперёд**, целиться на 2–4, а не «закрыть текущий гейт и остановиться».
2. Каждый новый блок — **самодостаточен** физически (проверено прогоном `pytest` на созданную папку сразу при выдаче пакета, не на словах).
3. Прежде чем объявлять инструмент/навык «приоритетом», подтверждать это рыночным источником (как в `docs/sonnet_reviews/2026-08-12-task-self-review-vs-real-interviews.md`), не интуицией методиста.
