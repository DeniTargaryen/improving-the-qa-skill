# Пакет на ревью Sonnet 5 — 15.08.2026

**Статус:** 15.08 вечер — ученик делает **3.3 как закрепление памяти** (слабо помнит patch с 13.08). Sonnet запускается **параллельно**: план + банк 30–60 вопросов под тупой внешний чат. 3.3 больше не «скип», но и не карьерный гейт юнитов.  
**Источник устного:** `03_api_framework/устный_опрос_в_gpt/вопросы_ответы_оценка.md` (режим «собеседник» / голосовой диалог, как на собесе).  
**Профиль:** ищет **Fullstack QA / AQA**, не разработчика. На текущей работе юнит-тестов **не было**: если e2e уже бьёт в ту же функцию сервиса — оставляли один e2e и просили разраба покрыть варианты (отлагательные условия, причины отказа) юнитами.

---

## 1. Что сделать Sonnet (обязательно)

1. Взять таблицу устного опроса ниже: пробелы — в календарь **раньше 4.x** (рядом с долгом скрининга Ozon 13.08: типы / mutable / аннотации). Формат: устные карточки, не новый блок кода на час.
2. **Пересмотреть статус 3.3.** 12.08 блок помечен обязательным по той же цитате Avito, что уже закрыла 2.2-А, плюс вакансия Ozon «разрабатывать моки» — у AQA это чаще mock-сервер, не `unittest.mock`. Ученик 15.08: 3.2 сдавал скрипя зубами (опять моки / юниты как у разработчиков); открыл 3.3 — весь блок снова про моки. Вопрос: для Fullstack QA / AQA ASAP 3.3 нужен как код или хватит устного шаблона из `2026-08-12-package-failure-and-market-review.md` §4?
3. Не раздувать `unittest.mock` новым блоком, если вердикт «достаточно 2.2-А + 3.2». Если оставить 3.3 — одной фразой **какой новый навык** сверх `patch` в 3.2 и `check_post_shape` в 2.3 (сейчас коуч этого навыка не видит).

Коуч **уже** ответил ученику: 3.3 не выполнять как обязательный гейт. Если Sonnet не согласен — аргумент в следующем пакете, не молча вернуть блок.

---

## 2. Устный опрос API (15.08) — сырьё + где дыры

Оригинал: `03_api_framework/устный_опрос_в_gpt/вопросы_ответы_оценка.md`.

Сильные места (не ломать, опираться в карточках): практика API (ключи/схемы/бизнес), Pydantic, график платежей с независимым expected в helper — это его реальный стек.

Дыры, которые **важнее 3.3** на следующем собесе Fullstack/AQA:

| Тема | Оценка / факт | Что вставить в устный drill |
|------|----------------|-----------------------------|
| Структура JWT | 4/10 — знал «три части», не назвал первую | Железо: `header.payload.signature` |
| JWT vs access/refresh | 4/10 — **смешал** три части JWT с парой access/refresh | Два уровня: из чего состоит JWT ≠ какие токены в схеме сессии |
| authentication vs authorization | не ответил | кто ты vs что разрешено |
| access / refresh | 6.5–7, сценарии refresh 5.5 / «—» | истечение access, refresh: валидный / истёк / reuse / logout |
| защищённый endpoint | «—» | 401 / 403 / валидный доступ / роли |
| авторизация кейсами | 5.5–6 — смешал валидацию входа, SMS и JWT | три полки: ввод → аутентификация → авторизация |

Остальное 7–8.5: структура ответа, статус, бизнес-график, стек Python/pytest/Pydantic. Не разжёвывать заново; добить формулировку «сначала структура: контракт → статус → данные → бизнес → негатив».

---

## 3. Претензия 3.2 / 3.3 — моки и юниты

**Его слова (15.08):** блок 3.2 сдавал скрипя зубами — опять моки, опять юнит-тесты, которые пишут разработчики. Перешёл в 3.3 — весь блок про моки. Это нормально? Ищет Fullstack QA / AQA, не разработчика. На работе юнитов не было: дублирование функции разработчика через e2e → один e2e, юниты на варианты — разрабу.

**Что уже закрыто кодом (не считать «он не умеет patch»):**

- 2.2-А: `Mock` / `return_value` (Б/В скип 12.08, рынок подтвердил).
- 3.2: живой 404 + `patch("negative_api.requests.get")` на 500. На понимание patch ушло ~30 мин; коуч тогда записал ASAP в `.cursor/rules/interview-prep-coach.mdc` §0.

**Что лежит в 3.3 фактически:**

```14:19:03_api_framework/3.3_unit_tests_with_mocks/posts_layer.py
def get_post(post_id: int) -> dict:
    return posts_client.get(path=f"/posts/{post_id}")
```

`check_post_shape` — повтор 2.3. `get_post`/`get_posts` — проброс в клиент без ветвления. TASK уже содержит готовый `with patch(...)` + `assert_called_once_with`. Это «напиши то, что в описании», не новый навык.

**Конфликт внутри пакета 12.08:**

- `2026-08-12-package-failure-and-market-review.md`: `unittest.mock` — не центр месяца, **один короткий блок (2.2-А) достаточно закрыт**; Mountebank first-class; 2.2-Б/В скип как второй проход того же паттерна.
- `2026-08-12-task-self-review-vs-real-interviews.md`: 3.3 снова **обязательный**, доказательства — цитата Avito «мокать платёжный шлюз» (уже на 2.2-А) и hh Ozon «разрабатывать моки» (для его резюме это Mountebank, не `patch` на однострочник).

Коуч: 3.3 = третий круг `patch` + повтор 2.3. Для его роли и работы это как раз «юнит на ту же функцию, которую уже бьёт e2e». Гейт недели 3 «3.1–3.3 иначе не готов к собесу» — пересмотреть.

---

## 4. Решение коуча ученику (15.08, второе — после уточнения)

Ученик: «как закрепление ок, с одного раза моки не запоминаю, сегодня уже слабо помню что писал 13.08». Команда коуча: **пиши 3.3 сейчас**. Sonnet параллельно не блокирует код.

Доп. заказ ученика к Sonnet: список **30–60 обязательных вопросов** для ИИ-чата не в Cursor, а в обычном бесплатном чате (модель тупая). Нужны: системный промпт «вставь первым», вопросы по одному, короткие эталоны, чтобы тупая модель не уехала в leetcode/юниты разработчика. Профиль Fullstack QA / AQA РФ 2026. Закрыть дыры: JWT, auth vs authorization, типы/mutable/аннотации (Ozon 13.08), pytest, HTTP, Mountebank vs `unittest.mock`.

---

## 5. Связанные файлы

- Устный сырой: `03_api_framework/устный_опрос_в_gpt/вопросы_ответы_оценка.md`
- Скрининг Ozon 13.08: `docs/sonnet_reviews/2026-08-13-ozon-hr-misses.md`
- Моки 12.08: `docs/sonnet_reviews/2026-08-12-package-failure-and-market-review.md`
- Статус 3.3 в таблице рынка: `docs/sonnet_reviews/2026-08-12-task-self-review-vs-real-interviews.md`
- Фидбек: `01_python_core/01_oop/STUDENT_FEEDBACK_for_sonnet.md`
