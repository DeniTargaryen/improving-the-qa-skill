# Ответы 4.3 (устно / своими словами)

См. `TASK.md`. Пиши сюда свои ответы 3–5 предложений на пункт — как эталоны в `docs/sonnet_reviews/2026-08-10-week1-and-1.4-review.md`.

## 1. Чем `@classmethod` отличается от `@staticmethod`

Твой ответ:

## 2. Как ты мокаешь внешний вызов и зачем это нужно (используй свой опыт из 2.2/3.3 + реальный опыт с Mountebank)

Твой ответ:

## 3. Зачем свои исключения (`ApiError`/`NotFoundError`) вместо голого `assert`

Твой ответ:

## 4. `scope` фикстур — какие бывают и в каком порядке вызываются при разных scope одновременно

Твой ответ:

## 5. Разница `PUT` и `PATCH`

Твой ответ:

## 6. Kafka: как ты проверял, что сервис слушает топик и публикует нужное сообщение (свой реальный опыт, не теория)

Твой ответ:

## 7. gRPC: как ты вызывал Go-сервисы напрямую (свой реальный опыт)

Твой ответ:
